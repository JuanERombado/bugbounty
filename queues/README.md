# Worker Queues

Worker queues are small JSON files that run bounded local-only jobs.

Use them for repeatable scans, test slices, fuzz campaigns, and duplicate-risk searches.

Do not put live-system targets, network scanners, or destructive commands in worker queues.
